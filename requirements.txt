# Pipeline dependencies for paysim_ecgb_pipeline.ipynb.
# Versions are those recorded by Section 1 of the notebook on the run whose
# outputs are committed, so pinning to them reproduces that environment.

# --- core ---
numpy==2.0.2
pandas==2.2.2
scipy>=1.13

# --- models ---
scikit-learn>=1.5
xgboost==3.3.0
lightgbm==4.6.0
catboost==1.2.10
imbalanced-learn>=0.12

# --- tuning ---
optuna==4.9.0

# --- explainability ---
shap==0.52.0
lime==0.2.0.1

# --- figures ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.22          # Optuna's optimisation-history and slice plots

# --- notebook ---
jupyterlab>=4.0
ipywidgets>=8.0
psutil>=5.9           # deployment evaluation (Section 11.10)
mongomock>=4.1        # in-process smoke test and benchmark

# The deployed application in Section 11 generates its own requirements.txt
# (flask, gunicorn, dash, pymongo and the subset of the above that it needs).
# It is intentionally separate: the container should not carry the training stack.
